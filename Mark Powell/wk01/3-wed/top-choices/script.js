var topChoices = ['blue', 'red', 'green', 'light blue', 'magenta', 'beige'];


// Create a loop that will iterate from 0 to 20
for (var i = 1; i < topChoices.length; i++) {
  var timesNine = i * 9;
  console.log('My #' + i + ' choice is ' + topChoices[i]);
}
